name: yoma_teams
description: "Aplicativo YOMA — sistema de segurança e denúncia discreta."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # Ícones padrão do iOS
  cupertino_icons: ^1.0.8

  # 🔥 Firebase (núcleo)
  firebase_core: ^4.2.1

  # Banco de dados em tempo real
  cloud_firestore: ^6.1.0

  # Autenticação (login/senha/telefone)
  firebase_auth: ^6.1.2

  # 📍 Localização (alerta com coordenadas)
  geolocator: ^14.0.2

  # 📱 Permissões do sistema (microfone, localização, armazenamento)
  permission_handler: ^12.0.1

  # 🔔 Notificações locais
  flutter_local_notifications: ^19.5.0

  # Vibração discreta
  vibration: ^3.1.4

  # Animações para interface lilás
  flutter_animate: ^4.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # 🖼️ ASSETS DO APLICATIVO YOMA
  assets:
    - assets/images/
    - assets/icons/
    - assets/splash/

  # 🔤 Fonte personalizada (caso você deseje ativar futuramente)
  # fonts:
  #   - family: Nunito
  #     fonts:
  #       - asset: assets/fonts/Nunito-Regular.ttf
  #       - asset: assets/fonts/Nunito-Bold.ttf
